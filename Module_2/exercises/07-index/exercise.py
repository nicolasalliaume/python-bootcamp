#############################################
#
# Sometimes you want to find an element inside
# a list. A good way to do it is by using the
# function 'index()'. index() takes as a parameter
# the element you are trying to find, and it is
# used as: the_list.index(<element>).
# index() will return the index in which the 
# element is stored.
#
# Use index() to find the position of the element 
# 'written'
#
#############################################

the_list = ['some', 'nonsense', 'elements', 'of', 'a', 'list', 'are', 'written', 'in', 'here']

print (  )